[core]
api_key = "admin123"
api_listen = "0.0.0.0:4200"

[default_model]
provider = "PROONE"
model = "MODEL"
api_key_env = "PKEY"

[hands.browser]
model_provider = "HPRO"
model = "HMODEL"
model_api_key_env = "HKEY"

[telegram]
enabled = true
bot_token_env = "TELEGRAM_BOT_TOKEN"

[network]
listen_addr = "0.0.0.0:4200"
